# LLM Wiki Canvas 0.1.0 release candidate

Status: **frozen release candidate; not published**
Freeze date: 2026-08-12

## Release notes

Version 0.1.0 establishes a local-first, file-native knowledge workbench for Markdown and Obsidian. It compiles deterministic relationship graphs, editable Canvas and Excalidraw artifacts, bounded Agent context, governed source intake, and hash-bound human-reviewed proposals. Repository Skills support Codex, Claude Code, Qoder, TRAE, and Tencent WorkBuddy without requiring MCP.

The 0.8 OKF Trust work adds Open Knowledge Format v0.2 checking and display for provenance, generated/verified actors, lifecycle, freshness, sources, and Attested Computation contracts. The Workbench shows origin, review, freshness, and checker findings without inventing a composite trust score. Attested Computation remains descriptive: the product never executes bundle SQL, Python, Skills, attesters, executors, or external commands.

Release-candidate gates include strict ISO 8601 validation, timestamp-based latest-verification selection, a pinned official OKF bundle regression, unknown-field preservation, desktop/mobile production E2E, package installation smoke tests, generated third-party notices, and Linux/macOS package CI on Node.js 20 and 22.

Known limits:

- This is an OKF v0.2 consumer and checker, not the canonical validator.
- It does not implement attestation execution, receipt verification, an attester ABI, or sandboxing.
- A declared `verified` event is surfaced as bundle metadata, not treated as external identity proof.
- The pinned official Acme Retail bundle has frontmatter in `log.md`; the checker intentionally reports the normative reserved-file violation.

## 中文摘要

0.1.0 是已经冻结但尚未发布的候选版本。它提供本地 Markdown/Obsidian 图谱、有限 Agent 上下文、受控资料导入和人工审查式变更，并增加 OKF v0.2 来源、复核、生命周期、时效及 Attested Computation 契约展示。产品不生成综合可信度分数，也绝不执行 Bundle 内 SQL、Python、Skill 或外部命令。

## GitHub presentation suggestions

These are suggestions only; no repository settings are changed by this release-candidate work.

- Homepage description: `Local-first visual compiler and review workbench for Agent-managed Markdown knowledge.`
- Social preview concept: a 1280×640 crop showing the relationship map on the left and the OKF Trust Inspector on the right, with the caption `Local files. Visible relationships. Reviewable trust.`
- Suggested release title if a maintainer later authorizes publication: `LLM Wiki Canvas 0.1.0 — local knowledge, bounded Agents, visible trust`.
