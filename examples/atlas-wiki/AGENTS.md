# Atlas Wiki rules

- Keep `sources/` factual and preserve the cited URL.
- Add synthesized ideas under `concepts/` and link them back to sources.
- Keep `index.md` concise; do not regenerate it wholesale.
- Append structural changes to `log.md`.
- Run `lwc lint` and inspect `Atlas.canvas` after link changes.
